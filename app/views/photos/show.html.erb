<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @photo.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @photo.description %>
</p>

	<%= link_to 'Edit', edit_surrounding_photo_path(@surrounding, @photo) %>
	<%= link_to 'Back', surrounding_photos_path(@surrounding) %>
